#Unique Pattern
class NamesCollection:
#Define some static variables here
    names = dict()

    @classmethod
    def init(cls):
        #Define any computation performed when assigning to a "new" object
        print 'NamesCollection object created'
        return cls
